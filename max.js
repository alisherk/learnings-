var points = [40, 100, 1, 5, 25, 10];

  var len = points.length;
  var max = -Infinity;
  while (len--) {
    if (points[len] > max) {
      max = points[len];
      console.log(max)
    }
  }









